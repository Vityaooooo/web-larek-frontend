export interface ProductData {
	title: string;
    description: string;
    image: string;
    category: string;
    price: number;
}

export interface ProductSettings {
	title: string;
    description: string;
    image: string;
    category: string;
    price: number;
}
